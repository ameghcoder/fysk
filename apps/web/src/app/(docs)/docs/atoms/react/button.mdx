---
title: "Button"
description: "A highly customizable button component with built-in state management for loading, success, and error feedback."
---

import { Button } from "@fysk/ui";

## Preview

<ComponentPreview
  name="Button"
  file="button"
  basePath="/registry/components/button"
  controls={{
    variant: {
      type: "select",
      options: ["primary", "secondary", "outline", "ghost", "link"],
      default: "primary",
    },
    size: {
      type: "select",
      options: ["sm", "md", "lg", "xl", "icon"],
      default: "md",
    },
    state: {
      type: "select",
      options: ["idle", "loading", "success", "error"],
      default: "idle",
    },
    loadingText: { type: "text", default: "Loading..." },
    successText: { type: "text", default: "Success!" },
    errorText: { type: "text", default: "Error!" },
    iconPosition: {
      type: "select",
      options: ["start", "end"],
      default: "start",
    },
  }}
>
  <Button>Click Me</Button>
</ComponentPreview>

## Installation

<CodeBlock
  language="bash"
  code="npx shadcn@latest add https://fysk.dev/r/button.json"
/>

## Usage

<CodeBlock language="tsx" code={`import { Button } from "@components/ui"

<Button variant="primary">Click Me</Button>`} />

## Examples

### Variants

Buttons come in several visual variants to establish hierarchy and context.

<ComponentPreview file="button-example-variants-01">
  <ButtonVariants />
</ComponentPreview>

### Size

Adjust the button size to fit different contexts.

<ComponentPreview file="button-example-sizes-01">
  <ButtonSizes />
</ComponentPreview>

### Action States

Built-in handling for loading, success, and error states.

<ComponentPreview file="button-example-states-01">
  <ButtonStates />
</ComponentPreview>

### Icons

Add icons at the start or end of the button label.

<ComponentPreview file="button-example-icons-01">
  <ButtonIcons />
</ComponentPreview>

### As Child

Compose with other components using the `asChild` prop.

<ComponentPreview file="button-example-as-child-01">
  <ButtonAsChild />
</ComponentPreview>

## Features

<Typography
  variant="list"
  data={[
    <Typography key="variants">
      <Typography variant="strong">Variants:</Typography> Different styles to
      suit various contexts.
    </Typography>,
    <Typography key="size">
      <Typography variant="strong">Size:</Typography> Adjust the button size to
      fit different contexts.
    </Typography>,
    <Typography key="state-management">
      <Typography variant="strong">State Management:</Typography> Integrated{" "}
      <Typography variant="inlineCode">
        `loading`, `success`, and `error`
      </Typography>{" "}
      states with customizable text and icons.
    </Typography>,
    <Typography key="hierarchical-icons">
      <Typography variant="strong">Hierarchical Icons:</Typography>{" "}
      Automatically resolves icons from the global{" "}
      <Typography variant="inlineCode">`FyskProvider`</Typography> context if
      not provided locally.
    </Typography>,
    <Typography key="composition">
      <Typography variant="strong">Composition:</Typography> Supports the{" "}
      <Typography variant="inlineCode">`asChild`</Typography> pattern for
      seamless integration with framework routers (like Next.js Link).
    </Typography>,
  ]}
/>

## Props

| Prop           | Type                                                         | Default     | Description                                           |
| -------------- | ------------------------------------------------------------ | ----------- | ----------------------------------------------------- |
| `variant`      | `"primary" \| "secondary" \| "outline" \| "ghost" \| "link"` | `"primary"` | The visual style of the button.                       |
| `size`         | `"sm" \| "md" \| "lg" \| "xl" \| "icon"`                     | `"md"`      | The size of the button.                               |
| `state`        | `"idle" \| "loading" \| "success" \| "error"`                | `"idle"`    | The current interaction state of the button.          |
| `asChild`      | `boolean`                                                    | `false`     | If true, the button will render as its child element. |
| `loadingText`  | `string`                                                     | -           | Text to display during the loading state.             |
| `successText`  | `string`                                                     | -           | Text to display during the success state.             |
| `errorText`    | `string`                                                     | -           | Text to display during the error state.               |
| `icon`         | `React.ReactNode`                                            | -           | Icon to display in the idle state.                    |
| `iconLoading`  | `React.ReactNode`                                            | -           | Custom icon for the loading state.                    |
| `iconSuccess`  | `React.ReactNode`                                            | -           | Custom icon for the success state.                    |
| `iconError`    | `React.ReactNode`                                            | -           | Custom icon for the error state.                      |
| `iconPosition` | `"start" \| "end"`                                           | `"start"`   | Position of the icon relative to the text.            |
