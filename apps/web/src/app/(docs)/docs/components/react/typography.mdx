---
title: "Typography"
description: "A comprehensive set of components for consistent text styling across the application."
---

import { Typography } from "@fysk/ui";

## Preview

<ComponentPreview
  name="Typography"
  file="typography"
  basePath="/registry/components/typography"
  controls={{
    variant: {
      type: "select",
      options: [
        "h1",
        "h2",
        "h3",
        "h4",
        "p",
        "blockquote",
        "lead",
        "large",
        "small",
        "muted",
        "strong",
        "inlineCode",
      ],
      default: "p",
    },
  }}
>
  <TypographyProvider>
    <Typography>The quick brown fox jumps over the lazy dog.</Typography>
  </TypographyProvider>
</ComponentPreview>

## Installation

<CodeBlock
  language="bash"
  code="npx shadcn@latest add https://fysk.dev/r/typography.json"
/>

## Usage

<CodeBlock
  language="tsx"
  code={`import { Typography } from "@/components/ui"

<Typography variant="h1">Heading</Typography>
<Typography variant="p">Body text.</Typography>`}
/>

## CSS Approach

If you want to style an entire block of HTML (like content from a CMS or Markdown), you can use the CSS-based approach.

1. Copy the following CSS into your global stylesheet (e.g., `globals.css`):

<CodeBlock
  language="css"
  code={`.fysk-typography {
    @apply text-foreground transition-colors duration-300;
}

.fysk-typography h1 {
@apply scroll-m-20 text-4xl font-extrabold tracking-tight lg:text-5xl;
}

.fysk-typography h2 {
@apply scroll-m-20 border-b border-border pb-2 text-3xl font-semibold tracking-tight mt-6 transition-colors first:mt-0;
}

.fysk-typography h3 {
@apply scroll-m-20 text-2xl font-medium tracking-tight mt-4;
}

.fysk-typography h4 {
@apply scroll-m-20 text-xl font-normal tracking-tight mt-2;
}

.fysk-typography p {
@apply leading-7 text-foreground/75 [&:not(:first-child)]:mt-2 tracking-wide;
}

.fysk-typography blockquote {
@apply mt-6 border-l-2 pl-6 italic text-foreground/75;
}

.fysk-typography ul {
@apply my-6 ml-6 list-disc [&>li]:mt-2 text-foreground/75;
}

.fysk-typography code {
@apply relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold text-foreground/75;
}

.fysk-typography table {
@apply w-full border-collapse border border-border mt-6 text-sm overflow-hidden rounded-lg;
}

.fysk-typography thead {
@apply bg-muted/50;
}

.fysk-typography th {
@apply border border-border p-3 text-left font-semibold;
}

.fysk-typography td {
@apply border border-border p-3 text-foreground/80;
}

.fysk-typography tr:nth-child(even) {
@apply bg-muted/20;
}

.fysk-typography strong {
@apply font-semibold text-foreground/75;
}

.fysk-typography small {
@apply text-sm font-medium leading-none text-foreground/75;
}`}
/>

2. Add the `.fysk-typography` class to the parent container of your content:

<CodeBlock
  language="tsx"
  code={`<div className="fysk-typography">
    <h1>My Content</h1>
    <p>This whole block is styled automatically.</p>
    <table>
      <thead><tr><th>Prop</th><th>Value</th></tr></thead>
      <tbody><tr><td>Status</td><td>Active</td></tr></tbody>
    </table>
</div>`}
/>

## Examples

### Default

Standard body and heading layout.

<ComponentPreview file="typography-example-default-01">
  <div className="flex flex-col gap-2">
    <Typography variant="h2">The Chronicles</Typography>
    <Typography variant="p">Once upon a time...</Typography>
  </div>
</ComponentPreview>

### Advanced Variants

Showcasing lists, blockquotes, and inline code.

<ComponentPreview file="typography-example-variants-01">
  <TypographyVariants />
</ComponentPreview>

## Features

<Typography
  variant="list"
  data={[
    <Typography key="semantic-html">
      <Typography variant="strong">Semantic HTML:</Typography> Dynamically
      switches the underlying HTML element (e.g.,{" "}
      <Typography variant="inlineCode">h1</Typography>,{" "}
      <Typography variant="inlineCode">p</Typography>,{" "}
      <Typography variant="inlineCode">blockquote</Typography>,{" "}
      <Typography variant="inlineCode">code</Typography>) based on the selected
      variant.
    </Typography>,
    <Typography key="responsive-sizing">
      <Typography variant="strong">Responsive Sizing:</Typography> Heading
      variants come with pre-configured fluid typography (e.g.,{" "}
      <Typography variant="inlineCode">h1</Typography> transitions between sizes
      on mobile/desktop).
    </Typography>,
    <Typography key="data-driven">
      <Typography variant="strong">Data Driven Lists:</Typography> The `list`
      variant accepts a `data` array prop for cleaner JSX when rendering bullet
      points.
    </Typography>,
    <Typography key="consistent-spacing">
      <Typography variant="strong">Consistent Spacing:</Typography> Built-in
      margin handling for standard document flow (e.g., `:not(:first-child)`
      logic for paragraphs and headers).
    </Typography>,
  ]}
/>

## Props

| Prop      | Type           | Default | Description                                      |
| --------- | -------------- | ------- | ------------------------------------------------ |
| `variant` | `string`       | `"p"`   | Visual style and semantic mapping.               |
| `as`      | `ElementType`  | -       | Manual override for the underlying HTML element. |
| `data`    | `React.Node[]` | -       | Array of items to render when `variant="list"`.  |

<Typography variant="blockquote">
  Accepts all standard HTML attributes for the underlying element.
</Typography>
